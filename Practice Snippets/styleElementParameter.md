# Style Element function with parameter

```js
    styleElement(styleProperty, styleValue) {
        styleProperty = this.element.style;
        Object.assign(styleProperty, styleValue)
    }
```

I want to try this.